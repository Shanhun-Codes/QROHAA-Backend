// import { describe, expect, it, jest } from '@jest/globals';

// jest.mock('src/prisma/prisma.service', () => ({
//   PrismaService: class PrismaService {},
// }));

// import { AgentAppController } from './agent-app.controller';

// describe('AgentAppController', () => {
//   it('returns dashboard leads with selected feedback', () => {
//     const leadsService = { findAllLeadsWithSelectedFeedback: jest.fn() };
//     const controller = new AgentAppController(leadsService as any);

//     controller.findLeads();

//     expect(leadsService.findAllLeadsWithSelectedFeedback).toHaveBeenCalledTimes(
//       1,
//     );
//   });
// });

// test
